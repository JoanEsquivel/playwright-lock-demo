# Playwright Cursor / agent standards

Copied from `JoanEsquivel/playwright-for-ui-interview-baseline` branch `feat/standards-kit`.

This folder is the agent kit only: Cursor rules, Claude skills/agents, `AGENTS.md`, Copilot mirrors, docs, and sync scripts. The SauceDemo/DummyJSON Playwright app, `package.json`, and lockfiles were not copied. Dependencies were not installed.

Start from `AGENTS.md` and `docs/agent-guide.md`.
