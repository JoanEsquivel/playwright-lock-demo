{
  "name": "playwright-lock-demo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "test": "playwright test",
    "test:setup": "playwright test --project=setup",
    "test:ui": "playwright test --project=ui",
    "test:e2e": "playwright test --project=e2e",
    "test:smoke": "playwright test --grep @smoke",
    "demo:race": "playwright test -c playwright.race.config.ts",
    "demo:locks": "playwright test --workers=4 --repeat-each=2 --retries=0",
    "test:headed": "playwright test --headed",
    "test:debug": "PLAYWRIGHT_HTML_OPEN=never playwright test --debug=cli",
    "report": "playwright show-report",
    "report:merge": "playwright merge-reports --reporter html ./blob-report",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . && tsc --noEmit",
    "lint:fix": "eslint . --fix",
    "sync:agents": "node scripts/sync-agent-config.mjs",
    "sync:agents:check": "node scripts/sync-agent-config.mjs --check"
  },
  "devDependencies": {
    "@playwright/cli": "0.1.19",
    "@playwright/test": "1.63.0",
    "@types/node": "^24.0.0",
    "dotenv": "^17.4.2",
    "eslint": "^10.10.0",
    "eslint-plugin-playwright": "^2.11.0",
    "typescript": "~5.9.3",
    "typescript-eslint": "^8.70.0",
    "zod": "^4.5.4"
  },
  "description": "Playwright test locks (1.63) demo: a shared session file, a race without locks, and the fix with { lock }"
}
