{
  "name": "playwright-cli-workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright CLI for this workspace (standards kit lives in cursor-standards/)",
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "playwright-cli": "playwright-cli"
  },
  "devDependencies": {
    "@playwright/cli": "0.1.19",
    "@playwright/test": "1.63.0"
  }
}
